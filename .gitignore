# Pasta de referências (documentos internos — NÃO publicar)
docs/refs/

# Configuração pessoal do usuário (gerada a partir de config.example.toml)
config.toml

# Segredos / ambiente
.env
.env.*
*.pem
*.key
secrets.json
# Exceção: os TEMPLATES .env.example dos perfis são versionados (só placeholders).
!.env.example
!**/.env.example

# Volumosos / gerados
.venv/
node_modules/
__pycache__/
