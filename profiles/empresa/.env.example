# .env.example — APENAS placeholders. NUNCA preencha com valores reais aqui.
# Em produção/desenvolvimento, carregue do cofre corporativo (Vault/OpenBao/Azure KV)
# em tempo de execução. O .env real deve estar no .gitignore e no .claudeignore.

# Exemplos de chaves esperadas (valores fictícios):
# USER:BEGIN id=projeto-env-vars
DATABASE_URL=postgresql://user:__FROM_VAULT__@localhost:5432/dbname
API_KEY=__FROM_VAULT__
SERVICE_ENDPOINT=https://servico.interno.exemplo/api
# USER:END
