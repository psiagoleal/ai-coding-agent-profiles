# .env.example — APENAS placeholders. NUNCA preencha com valores reais.
# Carregue do cofre (Doppler/Infisical/1Password) em tempo de execução.
# O .env real deve constar no .gitignore e no .claudeignore.

# USER:BEGIN id=projeto-env-vars
DATABASE_URL=postgresql://user:__FROM_VAULT__@localhost:5432/dbname
CLIENT_API_KEY=__FROM_VAULT__
# USER:END
